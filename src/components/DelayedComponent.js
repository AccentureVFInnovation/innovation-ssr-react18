import React from "react";

function DelayedComponent() {
  return (
    <>
      <h2>Delayed Component</h2>
      <p>
        In the context of React.js, concurrency refers to having more than one
        task in progress at once, and concurrent tasks can overlap depending on
        which is more urgent. For example, while I write this post.
      </p>
    </>
  );
}

export default DelayedComponent;
